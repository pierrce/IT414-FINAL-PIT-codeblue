Github repo - IT414-FINAL-PIT-codeblue


FOR LARAVEL BACKEND:

NOTE: Make sure you are inside the /backend so run the cd backend first

1. Open Xampp then click start and click the admin

1.1 setup create table rfid_monitoring

1.2 Then go back to vscode then in the terminal run this comman "php artisan migrate"

1.3 php artisan serve --host=0.0.0.0 --port=8000



FOR FRONTEND SVELTE:

NOTE: Make sure you are inside the /frontend so run the cd frontend first

1. Open terminal then run cd frontend

1.1 run "npm install"

1.2 npm run dev -- --host
